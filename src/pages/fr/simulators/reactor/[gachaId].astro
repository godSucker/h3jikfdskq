---
export const prerender = true;
import ReactorDetailPage from '@/components/pages/ReactorDetailPage.astro';
import { buildReactorStaticPaths } from '@/lib/reactor-static-paths';

export function getStaticPaths() {
  return buildReactorStaticPaths('fr');
}
---

<ReactorDetailPage {...Astro.props} />
